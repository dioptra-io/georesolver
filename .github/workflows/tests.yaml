name: Tests

on: [push]

jobs:

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - ecs-mapping-agent
          - score-agent
    steps:
      - uses: actions/checkout@v3
      - uses: dioptra-io/publish-docker-action@v1
        with:
          file: ./geogiant/dockerfiles/${{ matrix.image }}.dockerfile
          image: dioptra-io/geogiant/${{ matrix.image }}
          password: ${{ secrets.GITHUB_TOKEN }}
          platforms: linux/amd64
          push: ${{ github.actor != 'dependabot[bot]' }}

